{
  "name": "prompt-website",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/web dev & npm run -w apps/api dev",
    "build": "npm run -ws --if-present build",
    "test": "npm run -ws --if-present test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
